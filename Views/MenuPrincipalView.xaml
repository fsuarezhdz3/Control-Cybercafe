<UserControl x:Class="PuntoVenta2.Views.MenuPrincipalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#4A4A4A">
        <!-- Barra superior -->
        <Border Background="Black"
                Height="55"
                VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Botones izquierda -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="10,0,0,0">

                    <!-- Órdenes -->
                    <Button Click="BtnOrdenes_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="0,0,15,0"
                            ToolTip="Órdenes">
                        <Image Source="/Images/ordenes.png"
                               Width="28"
                               Height="28"/>
                    </Button>

                    <!-- Tiempos -->
                    <Button Click="BtnTiempos_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="Tiempos">
                        <Image Source="/Images/tiempos.png"
                               Width="28"
                               Height="28"/>
                    </Button>

                </StackPanel>

                <!-- Título -->
                <TextBlock Grid.Column="1"
                           Text="Menu Principal"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>

                <!-- Botones derecha -->
                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0">

                    <!-- Resumen -->
                    <Button Click="BtnResumen_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="0,0,15,0"
                            ToolTip="Resumen">
                        <Image Source="/Images/resumen.png"
                               Width="28"
                               Height="28"/>
                    </Button>

                    <!-- Cuenta -->
                    <Button Click="BtnCuenta_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="Cuenta">
                        <Image Source="/Images/cuenta.png"
                               Width="28"
                               Height="28"/>
                    </Button>

                </StackPanel>
            </Grid>
        </Border>

        <!-- Área principal -->
        <Grid Margin="0,55,0,0">
            <TextBlock Text="Selecciona una opción del menú superior"
                       Foreground="White"
                       FontSize="20"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.7"/>
        </Grid>
    </Grid>
</UserControl>